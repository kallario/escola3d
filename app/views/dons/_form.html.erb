<%= simple_form_for(@dom) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome_dom %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
