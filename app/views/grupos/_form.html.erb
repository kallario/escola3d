<%= simple_form_for(@grupo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :titulo_grupo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
